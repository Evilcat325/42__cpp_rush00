#ifndef FT_PLAYER_HPP
#define FT_PLAYER_HPP

class Player
{
private:
	/* data */
public:
	Player();
	~Player();
	Player(Player const &rhs);
	Player &operator=(Player const &rhs);
};

#endif /* FT_Player_HPP */
